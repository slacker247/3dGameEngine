#pragma once

namespace loaders
{
	class LoadImage
	{
	public:
		// Default Constructor
		LoadImage();
		// Copy Constructor
		LoadImage(const LoadImage &inLoadImage);
		// Destructor
		~LoadImage();

	protected:
	};
}